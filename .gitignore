# macOS
.DS_Store
Thumbs.db

# IDE / Editor
.idea/
*.swp

# Secrets / local config
.env
*.env.local

# PHP backend — contains DB credentials, deploy manually to Strato only
# DO NOT commit — upload api.php directly via FTP to gse.events/test/
api.php

# Node (if needed later)
node_modules/
dist/
